@{
  Layout = "_Layout";
}

<h1>Home Page</h1>

@using (Html.BeginForm("Index", "Places"))
{
  @Html.TextBox("searchRequest")

  <input type="submit" value="Go!"/>
}

@using System.Security.Claims 

@* NAVBAR WITH LOGIN/REGISTER LINKS AT TOP OR LOGOFF *@

@* @if (User.Identity.IsAuthenticated)
{
  @using (Html.BeginForm("LogOff", "Account"))
  {
    <button type="submit" class="btn btn-default">Log Out</button>
  }
}
else
{
  <button>@Html.ActionLink("Register", "Register")</button>
  <button>@Html.ActionLink("Log in", "Login")</button>
}


<button>@Html.ActionLink("Create an account", "Index", "Account")</button> *@